{
  "name": "@8columns/token",
  "description":"JavaScript library to create and validate token, inspired by JSON Web Token",
  "repository": {
    "type": "git",
    "url": "https://github.com/manit-tree/token"
  },
  "main": "./src/index.js",
  "type":"module",
  "scripts": {
    "dev": "bun ./src/index.js",
    "build": "vite build --config vite.config.js && vite build --config vite.config.min.js"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  },
  "dependencies": {
    "sha256": "^0.2.0"
  }
}
