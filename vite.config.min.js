import { defineConfig } from 'vite'

export default defineConfig({
    build: {
        outDir: './dist',
        minify: true,
        sourcemap: true,
        emptyOutDir: false,
        lib: {
            entry: './src/index.js',
            name:'sha256',
            formats: ['es','cjs','iife'],
            fileName: (format) => `[name].[format].min.js`
        }
    }
})